1. Por favor ejecutar en SQL SERVER el archivo con nombre (script.sql), que se encuentra dentro en la solución del proyecto.
2. Ir al archivo Web.config de la capa BookFlights.Web y cambiar el connectionString con la conexión al local del sql server donde se ejecuto el scrip.sql del punto 1.
3. Ir al archivo Web.config de la capa BookFlights.API_Integration y cambiar el connectionString con la conexión al local del sql server donde se ejecuto el scrip.sql del punto 1.

Nota: No se logro con el requerimiento solicitado, ya que tuve problemas al momento de consumir la API que me suministraron.
